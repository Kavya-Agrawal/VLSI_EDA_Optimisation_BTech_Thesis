# SYNAPSE Python training / export dependencies.
# CPU-only is fine for export + smoke tests; training wants a CUDA GPU.
numpy>=1.24
torch>=2.1              # CUDA build recommended for training
onnx>=1.15
onnxruntime>=1.17       # (onnxruntime-gpu optional)
# Optional: only needed for the gcn/sage/gat encoder backbones.
# The default 'aigconv' encoder needs only torch.
# torch-geometric>=2.5
