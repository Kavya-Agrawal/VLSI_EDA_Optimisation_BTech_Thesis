[project]
name = "openroad-evolution"
version = "0.1.0"
description = "Correctness-gated algorithm evolution for OpenROAD detailed placement"
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
